# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

- **全てのタスクを`[x]`にすること**
- 未完了タスク（`[ ]`）を残したまま作業を終了しない
- スキップは技術的理由のみ。理由を明記: `- [x] ~~タスク名~~（理由）`

---

## フェーズ1: 実装とテスト

- [x] `core/period-planner.js` に `toWholeSeconds` を追加し、`choosePeriod` で使う
- [x] `tests/unit/core/period-planner.test.js` に `toWholeSeconds` のテストを追加
- [x] `ui/messages.js` の `formatCycle` を秒の整数表示に変更
- [x] `ui/messages.js` の文言(画面の点灯色、QRコードの中身の説明、`v` `p` `c`)を変更
- [x] `core/defaults.js` の既定のメッセージを `NO WAR` に変更し、周期15秒のテストを追加

## フェーズ2: 品質チェック

- [x] `npm test` / `npm run lint` / `npm run typecheck`(121件)
- [x] ヘッドレスブラウザで確認(初期表示が NO WAR・メッセージの長さ15秒・`p=15`、ラベル、ウインドウの文言、60秒超は「74秒」+エラー)
- [x] 幅320pxで「画面の点灯色」の行が折り返したため、16進の入力欄を 6em → 5.25em(左右の余白8px)にして1行に収めた

## フェーズ3: ドキュメント更新

- [x] `docs/product-requirements.md`(既定値、メッセージの長さの表示、QRコードの中身)
- [x] `docs/functional-design.md`(作成画面の図・表示項目)
- [x] `docs/glossary.md`(1周分の所要時間の画面表示)
- [x] 実装後の振り返り

---

## 実装後の振り返り

### 実装完了日
2026-09-25

### 計画と実績の差分
- ラベルが「画面の点灯色」と長くなり、幅320pxで入力欄が次の行に折り返したため、入力欄の幅を詰めた(計画外)
- CSSの幅の置換で、同じ値だった `.field__input--number` まで変えてしまい、元に戻した。置換は対象のルールに範囲を絞って行う

### 学んだこと
- 表示用の秒数とURLの `p` を同じ関数(`toWholeSeconds`)から出すと、画面とQRコードの中身が食い違わない

### 次回への改善提案
- 文言を長くする変更では、幅320pxでの折り返しを最初に確認する
